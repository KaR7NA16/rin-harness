/**
 * Canonical-memory DTO compatibility surface.
 *
 * The stable records are owned by @rin/contracts; the memory domain re-exports
 * them so existing domain-internal relative imports keep one source of truth.
 *
 * @module @rin/memory
 */

export * from '@rin/contracts'
