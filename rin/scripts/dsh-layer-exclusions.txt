# rin-custom files inside the vendored dsh layer (packages/ vendor/ apps/ website/ native/ scripts/).
# During a dsh-layer sync these are RESTORED from rin's HEAD and never overwritten by upstream.
# Everything else under those directories is taken from the upstream ref.
#
# Keep this list minimal: each entry is rin code that must survive an upstream sync.

# --- rin brand (keep rin's rebrand of the dsh UI) ---
website/public/favicon.svg
website/public/wordmark.svg
apps/web/public/manifest.webmanifest
packages/client/ui-primitives/src/BrandWordmark.tsx
packages/client/ui-settings-models/src/onboarding-copy.ts

# --- rin test cleanup ---
apps/cli/tests/web-agent-presets.e2e.ts

# VERIFY (auto-synced from upstream, re-check after every sync):
#   packages/llm/llm-pi-ai/            — rin audit hardening (auth) ; upstream rc.7 rewrote this area, confirm fixes still apply
#   packages/e2b/e2b/package.json       — rin pinned version; upstream likely newer
#   packages/mcp/mcp-client/package.json— rin pinned version; upstream likely newer
#   scripts/                           — rin deleted some dev/CI scripts; upstream may re-add; decide whether to keep
#   pnpm-workspace.yaml (root, not synced) — rin workspace members + dependency overrides must be preserved
#   package.json / pnpm-lock.yaml (root, not synced) — rin metadata; pnpm install regenerates the lock
