# Desktop Scheduled Tasks

This vertical feature Module contains the Scheduled Tasks page, state, Local
Server client, UI types, presentation helpers, components, and colocated tests.

Desktop code outside this folder imports only `features/scheduledTasks/index.ts`.
Shared application transport, stores, controls, and UI primitives remain in
their existing cross-feature Modules.
